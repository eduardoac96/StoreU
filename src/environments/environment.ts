
export const environment = {
  production: false,
  //apiUrl: 'http://localhost:3000/' // Url to the mock API
  //apiUrl: 'https://localhost:5001/' // Url to the actual API if using VSCode
  apiUrl: 'https://localhost:44376/' // Url to the actual API if using Visual Studio
};